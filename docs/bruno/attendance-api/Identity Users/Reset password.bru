meta {
  name: Reset identity user password
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/identity/users/{{identityUserId}}/reset-password
  body: json
}

headers {
  X-CSRF-TOKEN: {{csrfToken}}
}

body:json {
  {
    "password": "{{newPassword}}"
  }
}
