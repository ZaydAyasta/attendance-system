meta {
  name: Set identity user status
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/identity/users/{{identityUserId}}/status
  body: json
}

headers {
  X-CSRF-TOKEN: {{csrfToken}}
}

body:json {
  {
    "isActive": false
  }
}
