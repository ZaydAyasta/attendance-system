meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
}

headers {
  X-CSRF-TOKEN: {{csrfToken}}
}

body:json {
  {
    "usernameOrEmail": "{{username}}",
    "password": "{{password}}",
    "rememberMe": true
  }
}
